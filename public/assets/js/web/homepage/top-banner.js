import {ready} from "../../dom-utils.js";


ready(() => {

    /*const variants = document.querySelectorAll("#page_content.homepage section.top_banner_container .top_banner .title_container .morph_sub_title_container .morph_word .variant");

    variants.forEach(variant => {
        const content = variant.querySelector('.content');
        variant.style.width = content.offsetWidth + "px";
    });
*/
})
